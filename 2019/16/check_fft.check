#include "fft.h"

#test signal_from_line_test
    char* line = "123";
    int signal[3];
    signal_from_line(line, signal);
    fail_unless(signal[0] == 1);
    fail_unless(signal[1] == 2);
    fail_unless(signal[2] == 3);
